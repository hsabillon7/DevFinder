<div class="main lista-candidatos">
    <h2>Lista de candidatos</h2>
    {{#if candidatos.length}}
    {{#each candidatos}}
    <div class="candidato">
        <div class="caja">
            <p class="etiqueta">Nombre</p>
            <p class="nombre">{{nombre}}</p>
        </div>
        <div class="caja">
            <p class="etiqueta">Correo electrónico</p>
            <p class="nombre">{{email}}</p>
        </div>
        <div class="caja">
            <a href="/uploads/hojasDeVida/{{hojaVida}}" class="btn btn-verde" target="_blank">Ver hoja de vida</a>
        </div>
    </div>
    {{/each}}
    {{else}}
    <p>No existen candidatos registrados en esta vacante.</p>
    {{/if}}
</div>